#Disena un programa que lea un numero flotante por teclado y muestre por pantalla el mensaje ✭✭
# El numero es positivo, solo si el numero es mayor o igual que cero.

num=float(input("Introduce un numero decimal: "))

if num > 0:
    print("El numero es positivo")